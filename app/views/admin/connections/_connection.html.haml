.clearfix.connected[connection]
  = image_tag "#{connection.provider}.png", :size => "48x48", :alt => "#{connection.provider}"
  %p= connection.username.split('/').last
  = link_to "remove connection", admin_connection_path(connection), method: :delete, confirm: 'Are you sure you want to remove this connection?', class: "remove ir"